# shell problem summary

## `${parameter-word}`
* if `parameter` is set and not `null`, then substitute the value of `parameter`.
* if `parameter` is set and but `null`, then substitute `null`.
* if `parameter` is unset, then substitute `word`.

